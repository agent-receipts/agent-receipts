# Cross-SDK Tests

This directory will contain cross-language receipt verification tests.

The goal is to ensure that a receipt signed by one SDK (e.g., Go) can be verified by every other SDK (TypeScript, Python) and vice versa. Test fixtures are generated from each SDK and validated across all implementations.

Status: placeholder -- tests coming soon.
